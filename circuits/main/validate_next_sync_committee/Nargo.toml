[package]
name = "validate_next_sync_committee"
type = "bin"
authors = [""]
compiler_version = "0.10.3"

[dependencies]
noirlib = {path = "../../noirlib"}
types = {path = "../../types"}
primitives = {path = "../../primitives"}