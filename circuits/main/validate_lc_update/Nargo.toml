[package]
name = "validate_lc_update"
type = "bin"
authors = [""]
compiler_version = "0.10.3"

[dependencies]
noirlib = {path = "../../noirlib"}
types = {path = "../../types"}
primitives = {path = "../../primitives"}